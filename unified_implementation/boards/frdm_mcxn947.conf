## Minimal FRDM build: WAD-in-flash + display + buttons.
## SD card / filesystem disabled (usdhc0 conflicts with sai0 pins).
# CONFIG_DISK_DRIVERS is not set
# CONFIG_FILE_SYSTEM is not set
# CONFIG_DISK_ACCESS is not set
# CONFIG_FAT_FILESYSTEM_ELM is not set

## Bluetooth is nRF-only
# CONFIG_BT is not set

## Audio via Zephyr I2S (SAI0)
CONFIG_I2S=y
CONFIG_DMA_TCD_QUEUE_SIZE=4

CONFIG_HEAP_MEM_POOL_SIZE=55000

CONFIG_THREAD_RUNTIME_STATS=y
CONFIG_THREAD_RUNTIME_STATS_USE_TIMING_FUNCTIONS=y
CONFIG_TIMING_FUNCTIONS=y
CONFIG_CPU_LOAD=y
CONFIG_CPU_LOAD_LOG_PERIODICALLY=1000
